{
  "name": "nativephp-build-action",
  "version": "0.1.0",
  "private": true,
  "description": "Community GitHub Action for building NativePHP Mobile Android applications.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
